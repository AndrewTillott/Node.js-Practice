<!DOCTYPE html>
<html>
    <head>
        <link href="/assets/styles.css" rel="stylesheet" type="text/css">
    </head>
    <body>
        <!--like a react compontent, it just pulls the relevant page into the code-->
        <%- include('partials/nav') -%> 
        <h1>Contact Us</h1>
        <p>You won't find a website better, anywhere else</p>
        <!--We are pulling the answer for the dept from the query string-->
        <!--You can see on ejs.ejs that we named the query string "queryString" and that is why it is named as such here-->
        <p><%= queryString.dept %> <%= queryString.person %></p>
        <form id="contact-form">
            <label for="who">Who do you want to contact?</label>
            <input type="text" name="who" value="<%= queryString.person %>">
            <label for="department">Which department?</label>
            <input type="text" name="department" value="<%= queryString.dept %>">
            <label for="email">Your Email</label>
            <input type="email" name="email" />
            <input type="submit" value="submit" />
        </form>
    </body>
</html>